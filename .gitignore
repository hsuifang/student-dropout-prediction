# Python
__pycache__/
*.py[cod]
.venv/
venv/
env/

# 模型產出：預設不入庫，但保留正式部署模型（小檔，方便 clone / Docker 直接用）
models/*.pt
models/*.joblib
!models/model.pt
!models/preprocessor.joblib

# 推論紀錄 (可能含敏感資訊)
results/inference_log.jsonl

# 原始 / 前處理後的學生資料 (含敏感屬性，不入庫)
data.csv
*_scaled.csv
notebooks/*.csv

# 系統檔
.DS_Store
.ipynb_checkpoints/
